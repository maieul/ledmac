\documentclass{article}
\usepackage{eledmac}
\footparagraph{A}
\begin{document}
\beginnumbering
\pstart
This \edtext{issue}{\Afootnote{Is it possible to set the footnote paragraph (e.g. from ) ragged right instead of justified? Is it possible to set the footnote paragraph (e.g. from ) ragged right instead of justified? Is it possible to set the footnote paragraph (e.g. from ) ragged right instead of justified?}} \edtext{issue}{\Afootnote{Is it possible to set the footnote paragraph (e.g. from ) ragged right instead of justified? Is it possible to set the footnote paragraph (e.g. from ) ragged right instead of justified? Is it possible to set the footnote paragraph (e.g. from ) ragged right instead of justified?}} has absolutely no priority for me, I don’t need a solution at the moment but maybe it could be a helpful feature.
\pend
\endnumbering
\end{document}
